{
  "id": "{{ namespace }}",
  "realm": "{{ namespace }}",
  "enabled": true,
  "eventsEnabled": true,
  "eventsExpiration": 7200,
  "displayName": "{{ project_name }}",
  "eventsListeners": ["jboss-logging"],
  "users": [
    {{ keycloak_users }}
  ],
  "roles": {
    "realm": [
      {{ keycloak_roles }}
    ]
  }
}
