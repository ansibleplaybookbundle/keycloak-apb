{
  "ssl-required": "{{ installation['ssl-required'] }}",
  "realm": "{{ installation['realm'] }}",
  "resource": "{{ installation['resource'] }}",
  "auth-server-url": "{{ installation['auth-server-url'] }}",
  "clientId": "{{ installation['resource'] }}",
  "url": "{{ installation['auth-server-url'] }}"
}
